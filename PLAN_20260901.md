# Plan del 1-sep · escrito la noche del 31 con todo corriendo

**Regla de trabajo nueva, de Maxi, y vale para todo lo de abajo:** *«priorizaría para correr en esta
PC cosas que no lleven mucho tiempo constante de CPU; para eso tenemos Colab. Si no hay Colab,
podemos ver la posibilidad de correr acá»*. Hoy se violó tres veces sin querer —la curva
techo-vs-RECUP estuvo **61 minutos al 93 %** y hubo que matarla— así que el **objetivo de
infraestructura** de mañana es que las mediciones de CPU también salgan por Colab.

---

## 0. ⚠ W-8 YA SE JUZGÓ la misma noche: colapsó igual y el culpable fue la INTERFAZ

`INFORME_W8_20260831.md`. Con `--rec-rank 0,008` —680× menos— el logit toma **4 y 7 valores
distintos sobre 3072**, saturación 0,9964 y 0,9876. **W-8-c: falló la interfaz, no la derivación del
peso.** La sección 1 de abajo queda como registro de lo que se planeaba; **no hay que re-correrla**.

**Entonces lo PRIMERO de mañana es otra cosa: la idea de Maxi de preguntar dos veces**, que es lo
único que abrió. Ver §2, y el control que falta está al final de `INFORME_DOS_VECES_20260831.md`:
comparar la precisión del desacuerdo contra la de la confianza de salida **al mismo nivel de
cobertura** (9,6 %). Es el análogo de D-4 para precisión, es barato, y sin él el 0,8980 no está
atribuido.

## 1. Lo que se planeaba y ya está hecho: juzgar W-8

`v03_s3` y `v03_s6`, `--abst slot` con `--rec-rank 0,008`. **Los criterios ya están escritos** en el
§7 de `PREREG_SLOT_ORDEN.md` (SHA de la enmienda `05e15659`) y no se tocan:

```
python3 juzgar_slot.py --trat v03_s3,v03_s6 --ctrl k03_s3,k03_s6
```

- **W-8-a** el logit **no colapsa** (más de 10 valores distintos, saturación < 0,95) → **falló mi
  DERIVACIÓN DEL PESO**, no la interfaz ni la idea. El criterio de igualar gradientes en la siembra
  queda desacreditado para `slot`.
- **W-8-c** colapsa igual → falló la **interfaz**, y eso **sí cierra la vía de la búsqueda**, con
  evidencia mucho más fuerte que la del 25-ago.

**Señal temprana al cierre del 31 (paso ~1000 de 3000): la abstención se MUEVE** —0,0078, 0,0039,
0,0293, 0,0273— en vez de estar clavada en 0,0000 como los tratamientos de 1,56 y 5,45. Apunta a
W-8-a. **No se adjudica nada con esto: es a mitad de corrida y está escrito acá sólo para que no
parezca una sorpresa mañana.**

## 2. La vía más prometedora, y es de Maxi: el DESACUERDO entre búsquedas

> *«si hace la búsqueda dos veces, si la respuesta es diferente una de otra, ¿qué sería eso?»*

**Por qué es la mejor propuesta abierta:** el techo de **0,7003** medido anoche acota a los lectores
que miran **el estado** —funciones de un punto—. El desacuerdo mide la **estabilidad del mecanismo**
alrededor de ese punto. Es otra familia de medidas y **puede pasar 0,70 sin contradecir nada**. Es la
única candidata con esa propiedad.

`desacuerdo_busqueda.py` quedó corriendo el 31 con los criterios D-1 a D-4 escritos adentro **antes**
del dato. Si D-4 (el control contra la confianza de salida) lo deja en pie y el AUC pasa de 0,55, el
paso siguiente es la versión **entrenada**, no post-hoc: dos proyecciones de query `qr1`/`qr2` y el
desacuerdo entrando en la pérdida. **Precedente que lo justifica: el blanco `error` da 0,65 post-hoc
y 1,0000 entrenado.**

## 3. El objetivo de infraestructura: `medir_en_colab.sh`

Las mediciones de CPU (sondas, curvas, jueces) tienen que poder correr en una T4 igual que el
entrenamiento. `tramo_abst.sh` ya sabe subir el código y un checkpoint; falta la variante que suba
**varios** checkpoints y corra un script de medición en vez de `entrenar.py`.

**Mientras no exista, la regla operativa es:** medir con `n` chico en la PC para ver si vale la pena,
y sólo entonces gastar. Y **siempre `python -u`**: hoy la curva corrió 61 minutos sin mostrar una
línea porque el buffer se comió la salida, y no se supo que estaba trabada hasta matarla.

## 4. La curva techo-vs-RECUP, que quedó a medias y es la que corrige una frase de la estrategia

`sonda_techo_curva.py`, ya arreglado (le faltaba `jit`: de **61 min sin terminar** pasó a **5 min por
checkpoint**). Falta correrla sobre las 10 unidades — **en Colab**, por la regla de arriba.

**Lo que decide:** el informe del 31 dice que *«la detección escala con la recuperación»*. T-4 lo
midió con dos checkpoints y dio **+0,0602** contra el ≥0,08 pedido, **pero esa comparación está
confundida** (12000 pasos contra 3000, base contra recompensa). Con diez puntos se ve la pendiente
real. **Si la pendiente es floja, hay que debilitar la frase «toda mejora pasa por recuperar
mejor»** — y eso cambia la estrategia de la línea entera.

**Dato ya obtenido y que hay que declarar:** el lector no lineal **corregido** (L3', que ahora sí
contiene al lineal) da **0,6345 contra 0,6881** del lineal sobre `n3_s0`. **No supera al lineal.**
Ojo con la lectura: al agregar 1024 features con el mismo barrido de `λ`, el lector más rico puede
perder por regularización, así que «no supera» es **evidencia débil de que no hay señal no lineal**,
no una prueba. Si importa, se arregla barriendo `λ` más fino para el no lineal.

## 5. Lo que NO hay que hacer mañana

- **No re-derivar el peso** con el criterio de igualar gradientes en la siembra. Ya falló una vez.
- **No cerrar la vía del slot** antes de leer W-8. La cláusula del §5 exige W-1, que quedó NO
  EVALUABLE.
- **No leer «el término bajó de log 2» como que ordenó** sin mirar cuántos valores distintos toma el
  logit. Es la trampa que el juez estuvo a punto de imprimir el 31, la **cuarta** del proyecto.
- **No correr nada de más de ~10 minutos de CPU en la PC** mientras haya Colab.

## 6. Estado al cierre del 31

| | |
|---|---|
| terminadas y juzgadas | `k03_s3` `k03_s6` `w03_s3` `w03_s6` — negativo, informe escrito |
| corriendo | `v03_s3` `v03_s6` (W-8) · `desacuerdo_busqueda.py` |
| listo y sin correr | la curva sobre 10 checkpoints, ya con `jit` |
| abierto sin diseño | el afilado `β(x)` por consulta (`NOTA_BUSQUEDA_UNIFORME_20260831.md`) |
